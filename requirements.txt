fastapi==0.115.6
uvicorn[standard]==0.32.1
httpx==0.28.1
pydantic==2.12.5
pydantic-core==2.41.5
python-dotenv==1.0.1
patchright>=1.49.0,<2.0.0
# Phase 2 agent runtime
aiohttp>=3.10,<4.0
# Phase 8 — Genesis job state machine
psycopg[binary]>=3.1
# Phase 9 — Output storage + Conduit session vault
boto3>=1.34
cryptography>=42.0
# Arize Phoenix / OpenInference runtime tracing (runtime/phoenix_tracing.py).
# Vendor-neutral OTLP rather than a Phoenix-specific SDK so a second exporter
# (Azure Monitor / Application Insights already accepts OTLP) is a config
# change, not a rewrite. Tracing is fail-open: if these are absent or the
# collector is unreachable, agent runs proceed untraced.
opentelemetry-sdk>=1.44.0
opentelemetry-exporter-otlp-proto-http>=1.44.0

# README.ralph.md — E4L Integration Architecture Residual

Generated by `spec-to-ralphprep` v2.0.0 from `SPEC-integration-architecture-final-stack.md` on 2026-08-12.

## What Ralph Will Build

This workspace coordinates four remaining code seams without rebuilding completed FinanceOS integrations or mixing repository ownership:

- `CHUNK_1_FINANCEOS_BOUNDARIES` — FinanceOS-only architecture boundary tests.
- `CHUNK_2_GENESIS_BLOB` — Genesis-only Azure Blob artifact backend.
- `CHUNK_3_GENESIS_COMPOSIO` — Genesis-only policy-gated non-financial Composio adapter.
- `CHUNK_4_GENESIS_VERIFY` — Genesis-only combined regression and exact-HEAD proof.

Four chunks are deliberate. The source spec has exactly four independently evidenced CODE seams; adding a fifth would be padding. Operational credential/live-service gates and the gated Document Intelligence decision are not build chunks.

## Owning Repositories

- FinanceOS: `C:\Users\Work\Desktop\vault\projects\E4L-FinanceOS\app`
- Genesis: `C:\Users\Work\Desktop\vault\projects\My Github\Genesis Agents`
- Ralph workspace: `C:\Users\Work\Desktop\vault\projects\My Github\Genesis Agents\ralph\integration-architecture-residual-ralph`

Each chunk modifies only its named application repository. Planning/state files stay here.

## Prerequisites

- Windows PowerShell
- FinanceOS: Node.js 22+, npm, TypeScript, ESLint, Vitest
- Genesis: Python, pip, pytest
- Real Azure Blob and Composio credentials are not required for planning or fake-client tests; their smoke tests are separate operational gates.

## Setup Before First Run

1. Read `SPEC-integration-architecture-final-stack.md`, `AGENTS.md`, and `.ralph/guardrails.md`.
2. In FinanceOS, run `npm install` without altering unrelated dirty files.
3. In Genesis, run `pip install -r requirements.txt` and `pip install pytest`.
4. Do not copy real secrets into this workspace. `.env.example` is a names-only scaffold reference.

## Start Ralph

From this workspace, run planning once:

```powershell
Get-Content -Raw .\PROMPT_plan.md | claude
```

Review the generated `IMPLEMENTATION_PLAN.md`, then run a bounded external loop:

```powershell
& 'C:\Users\Work\.claude\skills\ralph-wiggum-loop\scripts\ralph-loop.ps1' -Auto -MaxIterations 30
```

Do not launch a nested Claude process from inside an active Claude/Codex tool session.

## Validation

`AGENTS.md` contains one single-line PowerShell gate. It preserves the full FinanceOS `npm run verify`, then runs Genesis compile and its full pytest suite while deselecting only three exact pre-existing escrow-policy node IDs in `testing/test_job_lifecycle.py`: `test_escrow_settled_on_success_no_callback`, `test_escrow_released_on_failure`, and `test_timeout_triggers_escrow_release`. The rest of that file and suite still run.

The scaffold baseline is 606 passed and 15 skipped after those three exact deselections. This is a temporary baseline containment, not proof those escrow behaviors are correct. Re-run the three excluded tests after any escrow-policy change and remove each deselection as soon as its underlying baseline failure is repaired.

## Monitor

```powershell
Get-Content .\.ralph\state.md
Get-Content .\.ralph\progress.md -Wait
Get-Content .\.ralph\errors.log
```

## Completion and Operational Gates

`<promise>BUILD COMPLETE</promise>` means the four CODE chunks passed their exact-HEAD gates. It does not prove production credentials or live service readiness.

Live readiness separately requires sanitized evidence from:

- one real Genesis Azure Blob artifact round trip,
- one real approved non-financial Composio read action,
- the already-defined FinanceOS Airtable live-access gate.

Document Intelligence remains unbuilt unless the governing spec's evidence gate authorizes a new delta specification.

## Spec Files

```text
specs/
  01_CHUNK_1_FINANCEOS_BOUNDARIES.md
  02_CHUNK_2_GENESIS_BLOB.md
  03_CHUNK_3_GENESIS_COMPOSIO.md
  04_CHUNK_4_GENESIS_VERIFY.md
```

## Warnings from Spec Parsing

- The generic skill recommends 5–8 chunks, but the governing evidence-backed residual plan explicitly contains four. No artificial chunk was added.
- Cross-repository work requires the ownership routing in `AGENTS.md` and both prompts; no chunk may edit both repositories.
